# ============================================
# 开发工具依赖（lint / 类型检查 / 安全扫描 / 提交门禁）
# ============================================
# 这些工具不打包进运行时镜像，仅用于本地自检与 CI。
# 安装：pip install -r requirements-dev.txt
# 版本与 backend 容器内实际安装版本一致，避免本地/容器/CI 三方漂移。

# === 代码风格与静态检查 ===
ruff==0.16.1
black==26.5.1
isort==8.0.1
mypy==2.3.0

# === 安全扫描 ===
bandit==1.9.4
detect-secrets==1.5.0
pip-audit==2.10.1

# === 提交门禁 ===
pre-commit==4.6.1
